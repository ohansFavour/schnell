import React from "react";

import { StyledContainer, StyledHeader, StyledContent } from "../styled-components/styled-schnell";

const LongRange = () => {
  return (<StyledContainer>
    <StyledHeader>Long Range</StyledHeader>
    <StyledContent>
     Our single gateways can connect to your device up to 10km away.
    </StyledContent>
  </StyledContainer>);
};

export default LongRange;